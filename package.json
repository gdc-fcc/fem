{
  "devDependencies": {
    "vite": "^6.0.0",
    "vite-plugin-compression2": "^1.4.0"
  },
  "scripts": {
    "build": "vite build",
    "serve": "live-server dist",
    "deploy": "vercel deploy dist --prebuillt"
  }
}